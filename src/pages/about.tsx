import React from 'react';
import Layout from "@/components/layout/Layout";

const AboutPage = () => {
    return (
        <Layout>
            about
        </Layout>
    );
};

export default AboutPage;
